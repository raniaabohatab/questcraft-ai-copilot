export function ReminderBanner() {
  return (
    <aside
      className="sticky bottom-3 z-10 rounded-xl border border-gold/50 bg-navy px-4 py-3 text-center text-sm font-medium text-parchment shadow-lg sm:text-base"
      role="note"
    >
      These are just suggestions — you&apos;re always in control. Accept, revise, or ignore them.
    </aside>
  )
}
